import 'package:flutter/material.dart';
class HalloPage extends StatelessWidget {
  const HalloPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Hallo Page'));
  }
}